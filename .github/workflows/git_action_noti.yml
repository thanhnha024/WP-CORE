name: Wordpress Notifications Git Action

run-name: ${{ github.actor }} GitHub Actions 🚀

on: 
 push:
    branches:
      - feature-*
      - main
jobs:
  create-pull-request:
    runs-on: ubuntu-latest
    outputs:
      pull_number: ${{ steps.cpr.outputs.pull-request-url }}
    steps:
      - name: Auto create pull request when some one push code
        uses: actions/checkout@v4
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6

        with:
          commit-message: upadte new change on ${{ github.ref }}
          title: New update adjustment by ${{ github.actor }}
          author: ${{ github.actor }} <${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com>
          body: |
            Update new changes 💬:
            - Update new change on  ${{ github.ref_name }}
          branch: ${{ github.ref_name }}
          base: master
          reviewers: ThanhNha
          delete-branch: true

  notification-from-github:
    runs-on: ubuntu-latest
    needs: create-pull-request
    steps:
      - name: send telegram message on push
        uses: appleboy/telegram-action@master
        with:
          to: -1002247685770
          token: 6611988217:AAERCNw3lNbdpABmMztQudQXBuPcp1jsKdk
          message: |
            💥 Please check new commit 💥:

            ${{ github.actor }} created commit 🔑:
            Commit message 💬: ${{ github.event.commits[0].message }}

            Project 🚀: ${{ github.repository }}
            Project link 🔎: https://github.com/${{ github.repository }}
            See changes: https://github.com/${{ github.repository }}/commit/${{github.sha}}
            Pull request: https://github.com/${{ github.repository }}/pull/${{needs.create-pull-request.outputs.pull_number}}
